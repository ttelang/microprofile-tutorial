# Liberty Server Bootstrap Properties

# Suppress Derby manifest classpath warnings for locale JARs that don't exist in Maven
# These are optional internationalization files that aren't needed for basic operation
com.ibm.ws.logging.hideMessage=SRVE9967W
